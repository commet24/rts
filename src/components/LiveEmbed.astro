---
import { liveEmbed } from '@data/live';
import { siteConfig } from '@data/site.config';

const iframeTitle = `${siteConfig.live.title} ${siteConfig.siteName}`;
---

<section class="live-section" id="live" aria-labelledby="live-title">
  <div class="live-player-container">
    <div class="player-frame" aria-label="Плеер прямого эфира">
      {
        liveEmbed.hlsUrl ? (
          <video
            class="player-video"
            controls
            playsinline
            preload="metadata"
            aria-label={iframeTitle}
            data-hls-src={liveEmbed.hlsUrl}
          >
            <a href={liveEmbed.sourcePageUrl} target="_blank" rel="noreferrer">
              Открыть трансляцию
            </a>
          </video>
        ) : liveEmbed.hasGeneratedPlayer && liveEmbed.embedUrl ? (
          <iframe
            src={liveEmbed.embedUrl}
            title={iframeTitle}
            loading="eager"
            referrerpolicy="strict-origin-when-cross-origin"
            allow="autoplay; fullscreen; picture-in-picture; encrypted-media"
            sandbox="allow-scripts allow-same-origin allow-presentation"
            allowfullscreen
          />
        ) : (
          <div class="player-fallback">
            <span>Здесь может быть ваш плеер</span>
          </div>
        )
      }
    </div>
  </div>

  <div class="live-text-container">
    <h2 id="live-title">Смотреть онлайн</h2>
    <p class="live-description">
      Смотрите трансляцию нашего интернет-канала в адаптивном плеере.
    </p>
  </div>
</section>

<script>
  const videos = document.querySelectorAll<HTMLVideoElement>(
    'video[data-hls-src]',
  );

  for (const video of videos) {
    const hlsSrc = video.dataset.hlsSrc;

    if (!hlsSrc) {
      continue;
    }

    if (video.canPlayType('application/vnd.apple.mpegurl')) {
      video.src = hlsSrc;
      continue;
    }

    const { default: Hls } = await import('hls.js/light');

    if (Hls.isSupported()) {
      const hls = new Hls({
        enableWorker: true,
        lowLatencyMode: true,
      });
      hls.loadSource(hlsSrc);
      hls.attachMedia(video);
    }
  }
</script>
